import { FC } from "react";
import { Text, View } from "../Themed";
import { HeaderTitleProps } from "@react-navigation/elements";

const HeaderIndex: FC<HeaderTitleProps> = (props) => {
  return (
    <View>
      <Text>111</Text>
    </View>
  );
};

export default HeaderIndex;
